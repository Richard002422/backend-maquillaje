{
  "name": "aplication-ai-maquillaje",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:legacy-root": "vite",
    "build": "npm --prefix frontend run build",
    "build:legacy-root": "tsc && vite build",
    "preview": "npm --prefix frontend run preview",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
